@model IEnumerable<CommunityAssist2017.Models.BusinessRule>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BusinessRuleText)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BusinessRuleText)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BusinessRuleKey }) |
            @Html.ActionLink("Details", "Details", new { id=item.BusinessRuleKey }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BusinessRuleKey })
        </td>
    </tr>
}

</table>
